[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=root
WorkingDirectory=/var/www/stall_cart_project
ExecStart=/var/www/stall_cart_project/venv/bin/gunicorn --workers 3 --bind unix:/run/gunicorn.sock stall_cart.wsgi:application

[Install]
WantedBy=multi-user.target
